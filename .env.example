# Copia este archivo a .env.local y completa tus claves.
# .env.local está en .gitignore: NUNCA subas claves reales al repositorio.

# API de Claude (Anthropic) — https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Embeddings de Voyage AI — https://www.voyageai.com/
VOYAGE_API_KEY=

# Postgres + pgvector (Supabase) — cadena de conexión
DATABASE_URL=
